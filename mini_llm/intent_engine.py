# mini_llm/intent_engine.py


def load_training_data():
    # deploy nhẹ: không dùng sentence-transformers nữa
    return None


def detect_intent(text_norm: str):
    # trả về None hoặc "unknown" để pipeline vẫn chạy
    # tuỳ hr_pipeline của bạn xử lý sao
    return None
